import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function EarningsPage() {
  return (
    <PagePlaceholder
      title="Earnings"
      description="Daily, weekly, monthly, and total earnings, broken down per studio."
    />
  );
}
